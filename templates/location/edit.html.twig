{% extends 'base.html.twig' %}

{% block title %}Edit Location{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-1">
            <a href="{{ path('location_index') }}"><i class="fas fa-chevron-left fa-2x"></i></a>
        </div>
        <div class="col-11">
            <h1>Edit Location</h1>
        </div>
    </div>

    {{ include('location/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('location/_delete_form.html.twig') }}
{% endblock %}