{% extends 'base.html.twig' %}

{% block title %}PartnerRequest{% endblock %}

{% block body %}
    <h1>PartnerRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner_request.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ partner_request.email }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner_request.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ partner_request.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_request_index') }}">back to list</a>

    <a href="{{ path('partner_request_edit', {'id': partner_request.id}) }}">edit</a>

    {{ include('partner_request/_delete_form.html.twig') }}
{% endblock %}