{% extends 'base.html.twig' %}

{% block title %}Edit Partner Type{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-1">
            <a href="{{ path('partner_type_index') }}"><i class="fas fa-chevron-left fa-2x"></i></a>
        </div>
        <div class="col-11">
            <h1>Edit Partner Type</h1>
        </div>
    </div>

    {{ include('partner_type/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('partner_type/_delete_form.html.twig') }}
{% endblock %}