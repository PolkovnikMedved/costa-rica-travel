{% extends 'base.html.twig' %}

{% block title %}Edit Partner{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-1">
            <a href="{{ path('partner_index') }}"><i class="fas fa-chevron-left fa-2x"></i></a>
        </div>
        <div class="col-11">
            <h1>Edit Partner</h1>
        </div>
    </div>

    {{ include('partner/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('partner/_delete_form.html.twig') }}
{% endblock %}